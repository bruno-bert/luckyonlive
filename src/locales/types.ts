export type HomeLanguageType = {
  title: string,
  description: string,
  locale: string,
    headline: string
    subHeadline: string
    helper: string,
    buttonNormal: string
    buttonLoading: string,
    resultHeader: string,
    chatDisabled: string,
    urlInvalid: string,
    urlRequired: string,
    instruction: string
  }